<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base Application Theme -->
    <style name="Theme.TrackUrPill" parent="Theme.MaterialComponents.DayNight.DarkActionBar">

        <!-- Optional: set to transparent if your app is drawing behind the status bar. -->
        <item name="android:statusBarColor">
            @android:color/white
        </item>

        <!-- Optional: set for a light status bar with dark content. -->
        <item name="android:windowLightStatusBar">
            true
        </item>

        <!-- Remove the default ActionBar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Primary Colors -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryVariant">@color/primary_teal</item>
        <item name="colorAccent">@color/secondary_orange</item>

        <!-- Background and Surface -->
        <item name="android:windowBackground">@color/background_gray</item>
        <item name="colorSurface">@color/white</item>

        <!-- Text Colors -->
        <item name="android:textColorPrimary">@color/text_dark_gray</item>
        <item name="android:textColorSecondary">@color/text_light_gray</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorOnSurface">@color/text_dark_gray</item>
        <item name="colorControlNormal">@color/text_light_gray</item>
    </style>

    <!-- Theme for Buttons -->
    <style name="ButtonTheme" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/primary_blue</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
